<Window x:Class="SessionSaver.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SessionSaver"
        mc:Ignorable="d"
        Title="MainWindow" Height="521.885" Width="788.499" ResizeMode="NoResize" SizeToContent="WidthAndHeight">

    <StackPanel>
        <GroupBox Header="Current applications" VerticalAlignment="Stretch" Margin="10,10,10,10">
            <DataGrid Margin="10" Name="ApplicationDataGrid" ItemsSource="{Binding Path=ApplicationList}" HorizontalAlignment="Center" VerticalAlignment="Stretch" MaxHeight="400" MinHeight="400" AutoGenerateColumns="False" GridLinesVisibility="None" HorizontalGridLinesBrush="#FFE4DDDD" VerticalGridLinesBrush="#FFE4DDDD" SelectionMode="Single" CanUserResizeRows="False" ClipboardCopyMode="None" BorderThickness="0" BorderBrush="Transparent">
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" 
                    Color="#ebe2e1"/>
                </DataGrid.Resources>

                <DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <!--<Setter Property="BorderThickness" Value="0" />-->
                                <Setter Property="Foreground" Value="Black" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTemplateColumn CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Path=IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Application" Width="300" IsReadOnly="True" CanUserResize="True" MinWidth="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding Icon}" Width="18" Height="18" Margin="5, 0, 0, 0"/>
                                    <TextBlock Text="{Binding DescriptiveName}" Margin="5, 0, 0, 0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Binding="{Binding Title}" Header="Window title" IsReadOnly="True" MinWidth="100" Width="400" ClipboardContentBinding="{x:Null}"/>

                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <Button Command="{Binding SaveCommand}" Content="Save" HorizontalAlignment="Center"  VerticalAlignment="Center" Width="75"/>
        
        <DockPanel VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
            <StatusBar DockPanel.Dock="Bottom">
                
                <StatusBar.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="500" />
                                <ColumnDefinition Width="5" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="5" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </StatusBar.ItemsPanel>

                <StatusBarItem Grid.Column="0" HorizontalAlignment="Left">
                    <TextBlock Text="{Binding Path=Status}"/>
                </StatusBarItem>
                <Separator Grid.Column="1" />
                <StatusBarItem Grid.Column="2">
                    <TextBlock Text='{Binding Path="ApplicationCount", StringFormat="\{0\} applications in list"}'/>
                </StatusBarItem>
                <Separator Grid.Column="3" />
            </StatusBar>
        </DockPanel>
    </StackPanel>
</Window>
